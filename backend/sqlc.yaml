version: "2"
sql:
  - engine: mysql
    schema: db/migrations
    queries: db/queries
    gen:
      go:
        package: dbgen
        out: db/generated
        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: true

